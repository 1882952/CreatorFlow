● 项目是纯前端 SPA，无需构建，直接用浏览器打开即可：

  直接打开 creatorflow/index.html

  或用任意静态服务器：

  # Python
  cd F:/codePro/AIPro/comfyUI-workflow
  python -m http.server 8080

  # 然后访问 http://localhost:8080/creatorflow/

  新增的后端编排服务启动方式：

  cd services/orchestrator
  pip install -r requirements.txt
  python main.py
  # 服务运行在 http://localhost:18688